import React from 'react';

import s from './style.css';

const WIDTH = 20;

const Piece = ({height, color, position}) => {

  const style = {
    
  }

  return (
    <div className={s.container} style={style}>
    </div>
  )
}

export default Piece;
